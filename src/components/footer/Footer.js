import FootCard from "./FootCard";
import classes from "./Footer.module.css"

export default function Footer() {
  return (
    <section className={classes.cointainer} >
        <FootCard/>
    </section>
  );
}
